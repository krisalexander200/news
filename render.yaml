services:
  - type: web
    name: newsdrip-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /api/news
    envVars:
      - key: NODE_VERSION
        value: 20
